import { useState } from 'react'
import type { FormEvent } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import AuthFormShell from '../components/auth/AuthFormShell'
import { supabase } from '../lib/supabase'
const input = 'mt-2 w-full border border-blyver-ink/20 bg-transparent px-4 py-3 outline-none focus:border-blyver-champagne'
function SignInPage() { const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const location = useLocation(); const navigate = useNavigate(); const from = (location.state as { from?: string } | null)?.from ?? '/account'; async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(''); const form = new FormData(event.currentTarget); const { error: authError } = await supabase.auth.signInWithPassword({ email: String(form.get('email')), password: String(form.get('password')) }); setLoading(false); if (authError) return setError(authError.message); navigate(from, { replace: true }) } return <AuthFormShell eyebrow="BLYVER ACCOUNT" title="Welcome back." footer={<>New to Blyver? <Link to="/sign-up" className="underline">Create an account</Link></>}><form onSubmit={submit} className="space-y-5"><label className="block text-sm">Email<input required type="email" name="email" autoComplete="email" className={input} /></label><label className="block text-sm">Password<input required type="password" name="password" autoComplete="current-password" className={input} /></label>{error && <p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full bg-blyver-ink px-5 py-4 text-[11px] tracking-[0.16em] text-blyver-ivory disabled:opacity-60">{loading ? 'SIGNING IN…' : 'SIGN IN'}</button><Link to="/forgot-password" className="block text-center text-sm underline">Forgot your password?</Link></form></AuthFormShell> }
export default SignInPage
