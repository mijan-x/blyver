import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../features/auth/useAuth'
import { supabase } from '../lib/supabase'
function AccountPage() { const { user } = useAuth(); const navigate = useNavigate(); const name = typeof user?.user_metadata.full_name === 'string' ? user.user_metadata.full_name : 'Blyver customer'; async function signOut() { await supabase.auth.signOut(); navigate('/', { replace: true }) } return <main className="bg-blyver-ivory px-6 py-16 text-blyver-ink sm:px-10 sm:py-24"><section className="mx-auto max-w-7xl"><p className="text-[11px] tracking-[0.25em] text-blyver-champagne">BLYVER ACCOUNT</p><div className="mt-5 flex flex-col justify-between gap-6 border-b border-blyver-ink/10 pb-10 sm:flex-row sm:items-end"><div><h1 className="font-serif text-5xl sm:text-6xl">Welcome, {name}.</h1><p className="mt-4 text-sm text-blyver-charcoal/70">{user?.email}</p></div><button onClick={signOut} className="text-left text-[11px] tracking-[0.16em] underline">SIGN OUT</button></div><div className="mt-12 grid gap-px bg-blyver-ink/10 md:grid-cols-3"><Link to="/orders" className="bg-blyver-ivory p-8 transition hover:bg-blyver-ivory/60"><h2 className="font-serif text-2xl">Orders</h2><p className="mt-4 text-sm leading-7 text-blyver-charcoal/65">View your order history and current status.</p></Link><Link to="/wishlist" className="bg-blyver-ivory p-8 transition hover:bg-blyver-ivory/60"><h2 className="font-serif text-2xl">Wishlist</h2><p className="mt-4 text-sm leading-7 text-blyver-charcoal/65">Your saved Blyver pieces.</p></Link><Link to="/addresses" className="bg-blyver-ivory p-8 transition hover:bg-blyver-ivory/60"><h2 className="font-serif text-2xl">Addresses</h2><p className="mt-4 text-sm leading-7 text-blyver-charcoal/65">Save and manage your delivery addresses.</p></Link></div></section></main> }
export default AccountPage
